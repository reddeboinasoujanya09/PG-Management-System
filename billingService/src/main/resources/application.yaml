spring:
  application:
    name: billingService

  datasource:
    url: jdbc:postgresql://localhost:5432/billing_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update

  server:
    port: 8081