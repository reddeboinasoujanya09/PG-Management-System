spring:
  application:
    name: bookingService

  datasource:
    url: jdbc:postgresql://localhost:5432/booking_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update