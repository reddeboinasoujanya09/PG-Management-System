name: Booking Service Pipeline

on:
  pull_request:
    branches: [Master]
    paths:
      - 'bookingService/**'
      - 'common/**'

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: bookingService

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: pg_management_db
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: pass@123
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U admin -d pg_management_db"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - run: |
          if [ -f "pom.xml" ]; then
            mvn clean install
          elif [ -f "gradlew" ]; then
            chmod +x gradlew
            ./gradlew build
          else
            exit 1
          fi