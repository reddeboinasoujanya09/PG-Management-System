spring:
  application:
    name: roomService


  datasource:
    url: jdbc:postgresql://localhost:5432/pg_management_db
    username: admin
    password: "pass@123"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8085
